import * as React from 'react';

export interface ISearchBoxProps {
}

export default class SearchBox extends React.PureComponent<ISearchBoxProps> {
  public render() {
    return (
      <div>
        
      </div>
    );
  }
}
