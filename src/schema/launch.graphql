# import Rocket from "rocket.graphql"

type Launch {
    id: Int!,
    flight_number: Int!,
    launch_year: Int!,
    mission_name: String!,
    launch_success: Boolean!,
    rocketId: Int!,
}

type Query {
    launches: [Launch]
}

type Mutation {
    createLaunch(
        flight_number: Int!,
        launch_year: Int!,
        mission_name: String!,
        launch_success: Boolean!,
        rocket: Int!,
    ): Launch
}
